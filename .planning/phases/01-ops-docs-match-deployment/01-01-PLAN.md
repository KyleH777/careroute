---
phase: 01-ops-docs-match-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/AZURE.md
  - README.md
autonomous: false
requirements: [DOCS-01]
user_setup: []

must_haves:
  truths:
    - "docs/AZURE.md describes the deployment that exists: Terraform in infra/, centralus, VNet-injected Postgres with public access disabled, Key Vault-generated secrets read via managed identity, migrate/seed as Container Apps Jobs"
    - "Neither docs/AZURE.md nor README.md claims that nothing has been provisioned or that no infrastructure is declared"
    - "No doc instructs a reader to create Azure resources with ad-hoc `az ... create` (other than the ADR-0006 state-backend bootstrap script), open a Postgres firewall rule, or type/generate a production secret by hand"
    - "Every Azure section heading that RUNBOOK.md/INCIDENT-RESPONSE.md will link to exists with the exact names listed in Task 1"
  artifacts:
    - path: docs/AZURE.md
      provides: "Rewritten Azure operations guide (Terraform-as-built)"
    - path: README.md
      provides: "Corrected 'Infrastructure as code' and 'Deploying to Azure' sections"
  key_links:
    - from: docs/AZURE.md
      to: infra/*.tf
      via: "every resource name, SKU, region, secret name and job name in the doc matches the Terraform source"
    - from: docs/AZURE.md
      to: docs/adr/0006,0007,0008
      via: "links to the ADRs instead of restating (and contradicting) them"
---

<objective>
Replace docs/AZURE.md's original manual "az CLI + firewall rule + hand-typed secret" guide with an operator guide for the Terraform deployment that is live today, and correct the stale Azure/Terraform claims in README.md.

Purpose: Every later phase (DB roles, CI deploys, observability) amends these docs. They must start from the truth.
Output: rewritten docs/AZURE.md; two corrected README.md sections.
</objective>

<audit_findings>
Audit done 2026-09-25 against `terraform state list` (27 managed resources + 1 data source) and read-only `az ... show/list`. No secrets are recorded here, because .planning/ is public.

What is live (source of truth: infra/*.tf):
- Resource groups: `careroute-rg` (centralus, app), `careroute-env-infra-rg` (centralus, Azure-managed for the Container Apps environment), `careroute-tfstate-rg` (eastus, state storage, created by `infra/backend-setup/setup-backend.sh` and NOT in Terraform state).
- Network: `careroute-vnet` 10.20.0.0/16; `snet-apps` 10.20.0.0/23 (delegated Microsoft.App/environments); `snet-postgres` 10.20.2.0/24 (delegated flexibleServers); private DNS zone `careroute.postgres.database.azure.com` + VNet link.
- Postgres: `careroute-pg-<suffix>`, v16, B_Standard_B1ms, 32 GiB, `public_network_access_enabled = false` (live: publicNetworkAccess=Disabled, delegated to snet-postgres), admin `careroute_admin`, backup retention 7 days, no geo-redundancy. Database `careroute`.
- Key Vault `kv-careroute-<suffix>`: RBAC mode, soft delete 7 days, no purge protection. Secrets: `database-url`, `jwt-secret`, `demo-clinician-password`, `demo-coordinator-password`, all from `random_password`. The deployer has Secrets Officer; user-assigned identity `careroute-app-id` has Secrets User. There is a 90s `time_sleep` for RBAC propagation.
- Container Apps: env `careroute-env` (Consumption workload profile, VNet-integrated, logs to `careroute-logs`, 30-day retention, 0.5 GB/day cap). App `careroute-api`: Single revision mode, external ingress :8000, min 0 / max 2 replicas, 0.5 CPU / 1Gi, liveness `/health`, readiness `/ready`, APP_ENV=production, DATABASE_URL/JWT_SECRET from versionless KV references. Jobs `careroute-migrate` (`alembic upgrade head`) and `careroute-seed` (`python scripts/seed.py --demo-deployment`): manual trigger, 600s timeout, retry limit 0.
- Image pinned by `var.image` (currently `ghcr.io/kyleh777/careroute:sha-e15d28f`), and a validation rule forbids `:latest`.
- Live: API /health 200, /ready 200 (database reachable, env production).

Stale or false today:
- AZURE.md:5 "**Nothing here has been provisioned.**" is FALSE. It must be replaced with a short "What's live" status, not reworded.
- AZURE.md "Provisioning" (`LOC=eastus`, `az group create`, `az postgres flexible-server create`, `az ... db create`) describes a path that was never used. Postgres cannot be created in eastus on this subscription (ADR-0007). Remove the whole section.
- AZURE.md "Network access" (`firewall-rule create` for my IP) contradicts ADR-0007. With VNet injection there is no firewall. Remove it.
- AZURE.md "Connection string" / "JWT signing secret": a hand-built URL with `carerouteadmin` (actual login is `careroute_admin`), "App Setting", and `python -c secrets.token_urlsafe` are hand-typed secret steps that contradict ADR-0008.
- AZURE.md "Running migrations against Azure": `docker run ... DATABASE_URL=<password>@...` from a laptop cannot reach a private server and puts a secret in shell history. The real path is `az containerapp job start -n careroute-migrate`.
- AZURE.md "Backups on Azure": `az ... update --backup-retention 14` would drift from Terraform (`backup_retention_days = 7`). The retention change belongs in database.tf.
- AZURE.md "Cost control": `az consumption budget create` describes a budget that does not exist (no budget resource in Terraform). Cleanup via `az group delete --name careroute-rg` bypasses Terraform state and leaves the KV soft-deleted. The real teardown is `terraform destroy`.
- README.md:322-323 "`infra/main.tf` configures the backend only; no infrastructure is declared yet." is FALSE (7 .tf files, 27 resources).
- README.md:327 "Nothing there has been provisioned" is FALSE and contradicts the live-demo link on README.md:15.
- README.md:305 "Operations" bullet says of the runbook "Every procedure was run against this stack". That is not true for the Azure steps (RUNBOOK.md:9 says so itself). Qualify it.

Confirmed correct, keep as-is: no doc mentions AKS/Kubernetes/kubectl/helm (grep clean). The only East US references are the AZURE.md provisioning block (being removed) and the state backend, which really is in eastus (ADR-0007).

Not a docs issue but flag to the user: `infra/careroute.tfplan` exists locally. It is git-ignored, but plan files embed every secret (ADR-0008). main.tf's header comment tells operators to write a plan file and does not say to delete it.
</audit_findings>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/AZURE.md
@README.md
@infra/main.tf
@infra/variables.tf
@infra/network.tf
@infra/database.tf
@infra/keyvault.tf
@infra/containerapps.tf
@infra/outputs.tf
@infra/backend-setup/setup-backend.sh
@docs/adr/0001-one-shot-migration-before-api.md
@docs/adr/0006-terraform-state-backend.md
@docs/adr/0007-private-postgres-in-centralus.md
@docs/adr/0008-secrets-generated-into-key-vault.md
</context>

<cost_rule>
The user asked that nothing incur cost unless absolutely necessary. During execution:
- NO `terraform apply`/`destroy`, NO `az containerapp job start`, NO `revision restart`, NO secret rotation, NO restore.
- Allowed (free): reading files, `terraform validate`/`fmt -check`, `az <cmd> --help`, read-only `az ... show/list`, and reading Microsoft Learn docs.
- Do not curl the live API. It scales to zero, and waking it bills compute.
- Commands in the doc are verified for syntax against `--help` and the .tf source, not by running them. The doc must say so plainly.
</cost_rule>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>How should the docs present the Azure restore path?</decision>
  <context>ROADMAP success criterion 2 says restores go "through an in-VNet job instead of laptop psql". Today no restore tooling exists in Azure. The app image is a hardened image with no pg_dump/pg_restore, and no job runs them. PITR (`az postgres flexible-server restore`) works, but it creates a server outside Terraform state, and the cutover would mean changing Terraform-managed `database-url`. This is a docs phase: adding new infrastructure would be scope creep and has a cost.</context>
  <options>
    <option id="document-pitr-plus-gap">
      <name>Document PITR as the Azure restore path and name the gaps (recommended)</name>
      <pros>Honest and costs nothing. PITR is Azure's native recovery. The procedure covers restoring to a new private server in the same VNet, verifying it through an in-VNet one-off job (the migrate job with a `--command python -c` override: the image has python + psycopg), and cutting over by importing into Terraform or re-pointing `database-url`. Logical-dump restore to Azure is listed as a known gap with a follow-up.</pros>
      <cons>Criterion 2's "in-VNet restore job" is met for verification queries, not for pg_restore of a logical dump.</cons>
    </option>
    <option id="add-restore-job">
      <name>Also add a `careroute-dbtools` job (postgres:16 image) in Terraform</name>
      <pros>Gives a true in-VNet pg_dump/pg_restore path.</pros>
      <cons>Infra change in a docs phase. It needs `terraform apply` (the user said avoid cost), a storage location for dumps, and more secrets exposure. Better as its own phase or inserted phase 1.1.</cons>
    </option>
  </options>
  <resume-signal>Select: document-pitr-plus-gap or add-restore-job</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Rewrite docs/AZURE.md as the as-built Terraform operations guide</name>
  <files>docs/AZURE.md</files>
  <read_first>docs/AZURE.md, infra/*.tf, infra/backend-setup/setup-backend.sh, docs/adr/0001, 0006, 0007, 0008</read_first>
  <action>
Replace the file. Keep the repo's doc voice (short declarative sentences, tables for lookups, fenced bash one-command-per-block). Use these exact H2 headings, in this order, because plan 01-02 links to them:

1. `# Azure deployment (Terraform)`: one paragraph on what this is and that `infra/` is the source of truth. Then a **"What's live"** callout that replaces the old "Nothing here has been provisioned" line: live since 2026-09-24, in centralus, managed by Terraform (27 resources in state), with the public URL (already public in README.md:15). Say to get names from `terraform output` (api_url, resource_group, key_vault_name, postgres_fqdn, jobs). Do NOT hardcode the random suffix, subscription ID or any secret.
2. `## Architecture`: a table of each resource (from <audit_findings>) with its purpose. One paragraph each on the network (no public DB endpoint, no firewall rules, private DNS, link ADR-0007) and on secrets (random_password → Key Vault → user-assigned identity → versionless refs, link ADR-0008). Name the three resource groups, and say why state is in eastus while the app is in centralus (ADR-0007: Postgres Flexible Server restricted in eastus/eastus2/westus2).
3. `## First-time deployment`: backend bootstrap (`setup-backend.sh`, ADR-0006) → `export ARM_SUBSCRIPTION_ID=...` → `terraform init -backend-config=backend.hcl` → `terraform plan -out=careroute.tfplan` → `terraform apply careroute.tfplan` → **delete the plan file** (`rm careroute.tfplan`; it embeds every secret) → `az containerapp job start -g careroute-rg -n careroute-migrate` → wait for the execution to succeed → `az containerapp job start ... -n careroute-seed` (ADR-0009 demo). Include how to check a job execution result (`az containerapp job execution list -g careroute-rg -n careroute-migrate -o table`).
4. `## Deploying a new image`: the manual ADR-0001-ordered procedure until Phase 3 automates it. First run the migration with the NEW image via `az containerapp job start -g careroute-rg -n careroute-migrate --image ghcr.io/kyleh777/careroute:sha-<new>` and confirm success. Then `terraform apply -var image=ghcr.io/kyleh777/careroute:sha-<new>`. Warn explicitly that a plain `terraform apply` with a new image updates the job and the app at once, so the app can roll before migration. Note that `:latest` is rejected by variable validation.
5. `## Migrations and seeding`: jobs, manual trigger, retry limit 0 (a failed migration needs a human), 600s timeout. Keep the useful `alembic upgrade head --sql` review tip, rewritten to run locally against the Compose DB rather than a prod URL.
6. `## Logs`: `az containerapp logs show -g careroute-rg -n careroute-api --type console --tail 100` (and `--follow`), job logs via `az containerapp job logs show` (verify the command exists with `--help` and drop it if not), and a Log Analytics query on `ContainerAppConsoleLogs_CL` in workspace `careroute-logs`. State the 30-day retention and 0.5 GB/day cap (ingestion stops past it). Verify the table name against Microsoft Learn.
7. `## Querying the database from inside the VNet`: laptop psql cannot reach the server (ADR-0007). Document a one-off read-only query by starting the migrate job with a command override, e.g. `az containerapp job start -g careroute-rg -n careroute-migrate --command python --args -c "<python using sqlalchemy/psycopg and os.environ['DATABASE_URL']>"`. Confirm the flags against `az containerapp job start --help` (they exist as of 2026-09-25). Warn that the command text is kept in execution history (never put secrets in it), and that each execution bills a few seconds of consumption. Provide ONE worked example: the "select email, role, is_active from users where email=..." lookup. Mark it "syntax-checked, not executed".
8. `## Secrets and rotation`: table of the four KV secrets → env var → which workload reads it. Rotation = `terraform apply -replace=random_password.<name>`, which writes a new KV version. The versionless ref means no Terraform change to the app, BUT running replicas keep the old value until they restart. Follow with `az containerapp revision restart -g careroute-rg -n careroute-api --revision <latest>` (latest revision from `az containerapp revision list`). Jobs pick up the new value on their next execution. Specifics: `jwt_secret` rotation logs everyone out; `postgres_admin` rotation changes the server password AND `database-url` in one apply, so the API fails DB calls (/ready 503) between the apply and the restart. Keep that window short and do it off-hours. Before writing, check against Microsoft Learn whether Container Apps also auto-refreshes KV references periodically; if it does, mention it but say not to rely on it. Never a hand-typed secret. To read a secret for break-glass, use `az keyvault secret show` (requires Secrets Officer, and it prints the secret to the terminal).
9. `## Backups and restore`: PITR, 7-day retention (set in `infra/database.tf` `backup_retention_days`, change it there, not with `az ... update`). Restore procedure per the checkpoint decision: `az postgres flexible-server restore` to a NEW server, which inherits private access (verify the VNet/subnet/private-DNS behaviour and flags against `--help` and Learn). Verify it with the in-VNet query from section 7 pointed at the new host. Cutover options: `terraform import`/re-point, spelled out as the documented gap. State that `scripts/backup.sh`/`restore.sh` are Compose-only; a logical dump of the Azure DB has no in-VNet runner yet (known gap, if option document-pitr-plus-gap was chosen).
10. `## Cost`: what bills. Postgres B1ms + 32 GiB (the free-offer terms paragraph can stay, trimmed), Container Apps consumption with scale-to-zero (min_replicas 0, cold start), Log Analytics capped at 0.5 GB/day, Key Vault operations, state storage. Say no budget alert is provisioned by Terraform. Offer `az consumption budget create` as an optional manual step, labelled as not part of the deployment, OR drop it. Do not claim a budget exists. Stopping Postgres (`az postgres flexible-server stop`) makes /ready 503 and it auto-starts after 7 days.
11. `## Teardown`: `terraform destroy` from infra/. Key Vault is soft-deleted for 7 days (purge_soft_delete_on_destroy=false), so a same-name redeploy needs a purge or a new suffix. The state RG is untouched and locked (ADR-0006). Warn: do NOT `az group delete careroute-rg`, which bypasses state.

Remove entirely: the eastus `az group create`/`az postgres flexible-server create`/`db create` block, the firewall-rule section, the hand-built connection string with a password placeholder, the `python -c secrets.token_urlsafe` JWT step, the laptop `docker run ... alembic upgrade head` against Azure, and `az group delete`.
  </action>
  <verify>
grep -nE "Nothing here has been provisioned|firewall-rule|LOC=eastus|carerouteadmin|token_urlsafe|az group delete|flexible-server create|App Setting" docs/AZURE.md   # expect no output
grep -nE "^## " docs/AZURE.md   # expect exactly the 10 H2s above, in order
grep -nE "centralus|careroute-migrate|careroute-seed|Key Vault|public_network_access_enabled|terraform destroy|ADR-0007|ADR-0008" docs/AZURE.md   # each present
az containerapp job start --help >/dev/null && az containerapp revision restart --help >/dev/null && az postgres flexible-server restore --help >/dev/null   # flags used exist (free)
  </verify>
  <acceptance_criteria>
    - docs/AZURE.md contains none of: "Nothing here has been provisioned", "firewall-rule", "LOC=eastus", "carerouteadmin", "token_urlsafe", "az group delete", "flexible-server create"
    - docs/AZURE.md contains "What's live", "centralus", "careroute_admin" (if the login is named at all), "careroute-migrate", "terraform apply -replace=random_password", "revision restart", "terraform destroy"
    - Every `az` subcommand and flag in the doc appears in the corresponding `--help` output
    - Every resource/job/secret name matches infra/*.tf exactly
    - No subscription ID, random suffix value, or secret value appears in the doc
  </acceptance_criteria>
  <done>AZURE.md describes only the Terraform-built deployment, with the "nothing provisioned" line replaced by a truthful "What's live" status.</done>
</task>

<task type="auto">
  <name>Task 2: Correct README.md's Azure and Terraform claims</name>
  <files>README.md</files>
  <read_first>README.md (lines 1-20, 290-335), docs/AZURE.md (as rewritten)</read_first>
  <action>
- "Infrastructure as code (Terraform)" section: replace "`infra/main.tf` configures the backend only; no infrastructure is declared yet." with a 2-4 sentence summary of what infra/ declares (Container Apps API + migrate/seed jobs, VNet-injected Postgres 16, Key Vault with generated secrets, Log Analytics, all in centralus). Link docs/AZURE.md and ADRs 0006-0008. Keep the bootstrap commands.
- "Deploying to Azure" section: delete "Nothing there has been provisioned — those commands create billable resources under your subscription." Replace with: the demo is live (link the URL in README.md:15), deployed and changed only through Terraform, and applying Terraform in your own subscription creates billable resources (see AZURE.md → Cost). Replace "Setup for Azure Database for PostgreSQL Flexible Server, including free-tier eligibility rules, TLS, migrations, and point-in-time restore" with a description of the new AZURE.md. Keep the "same image in both environments / DATABASE_URL" sentence, adding that in Azure the value comes from Key Vault.
- "Operations" bullet for the runbook: change "Every procedure was run against this stack." to say the Compose procedures were run and the Azure procedures are syntax-checked (or whatever plan 01-02 records).
- Leave everything else in README.md untouched.
  </action>
  <verify>
grep -nE "no infrastructure is declared|Nothing there has been provisioned|Every procedure was run" README.md   # expect no output
grep -nE "docs/AZURE.md|centralus|Key Vault" README.md   # present
  </verify>
  <acceptance_criteria>
    - README.md contains neither "no infrastructure is declared yet" nor "Nothing there has been provisioned"
    - README.md's Deploying section is consistent with the live-demo link at the top
    - git diff README.md touches only the three named sections
  </acceptance_criteria>
  <done>README no longer contradicts itself or the live deployment.</done>
</task>

</tasks>

<verification>
- [ ] `grep -rnE "Nothing (here|there) has been provisioned|no infrastructure is declared|firewall-rule|LOC=eastus" README.md docs/AZURE.md` returns nothing
- [ ] `cd infra && terraform fmt -check && terraform validate` still pass (docs-only change, as a sanity check that no .tf was touched)
- [ ] `git diff --stat` lists only docs/AZURE.md and README.md
- [ ] No cost-incurring command was executed (see <cost_rule>)
</verification>

<success_criteria>
- ROADMAP Phase 1 criterion 1 met: AZURE.md goes from backend bootstrap to a live deployment using only Terraform + Container Apps Jobs; it states centralus, VNet-injected Postgres with no public endpoint or firewall rules, and Key Vault-generated secrets; no firewall-rule or hand-typed-secret steps remain
- The user's explicit check is met: the "nothing here has been provisioned" line is replaced with what is actually live (and README's matching line likewise)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ops-docs-match-deployment/01-01-SUMMARY.md`
</output>
