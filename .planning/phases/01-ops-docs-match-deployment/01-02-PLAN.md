---
phase: 01-ops-docs-match-deployment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/RUNBOOK.md
  - docs/INCIDENT-RESPONSE.md
  - docs/BACKUP-RESTORE.md
autonomous: true
requirements: [DOCS-02, DOCS-03]
user_setup: []

must_haves:
  truths:
    - "RUNBOOK.md has an Azure path for: first five minutes (health/readiness), logs, migrations, user lookup/lockout, JWT_SECRET rotation, bad-deploy rollback, and restore. Each links to the AZURE.md section headings from plan 01-01"
    - "RUNBOOK.md contains no firewall-rule check and no 'App Setting' secret instruction"
    - "INCIDENT-RESPONSE.md's JWT_SECRET and DB-password rotation steps name `terraform apply -replace=random_password.jwt_secret` / `random_password.postgres_admin` and the `az containerapp revision restart` step"
    - "The three docs contain no instruction that contradicts ADR-0006, 0007 or 0008"
  artifacts:
    - path: docs/RUNBOOK.md
      provides: "Azure equivalents alongside the Compose procedures"
    - path: docs/INCIDENT-RESPONSE.md
      provides: "Rotation/containment steps via Terraform + Key Vault"
  key_links:
    - from: docs/RUNBOOK.md
      to: docs/AZURE.md
      via: "links to the AZURE.md H2 anchors (Logs, Secrets and rotation, Querying the database from inside the VNet, Backups and restore, Deploying a new image)"
    - from: docs/INCIDENT-RESPONSE.md
      to: docs/RUNBOOK.md
      via: "Stabilize table and Contain list point at the RUNBOOK Azure rows"
---

<objective>
Give every operational procedure in RUNBOOK.md and INCIDENT-RESPONSE.md a correct Azure path, remove the firewall/App-Setting instructions, and route credential rotation through Terraform + Key Vault. Finish with a cross-doc audit against ADR-0006/7/8.

Purpose: DOCS-02 and DOCS-03, i.e. ROADMAP Phase 1 criteria 2-4.
Output: updated RUNBOOK.md and INCIDENT-RESPONSE.md; minor link fix in BACKUP-RESTORE.md if needed.
</objective>

<audit_findings>
Stale lines found 2026-09-25:
- RUNBOOK.md:9: "Not exercised here: ... the Azure steps". Update it to state exactly which Azure steps were executed and which were only syntax-checked (per the cost rule, expect "syntax-checked").
- RUNBOOK.md:16-18: "docker compose steps become their Azure equivalents (see AZURE.md)". There are no equivalents today. Add them.
- RUNBOOK.md:80: "Azure: connection refused/timeout → Check ... the firewall rule allows the app's outbound IP (AZURE.md → Network access)". Contradicts ADR-0007 and the anchor is being removed.
- RUNBOOK.md:125: "(AZURE.md → JWT signing secret)". The anchor is being removed, and the fix in Azure is Terraform/KV, not "set it and restart".
- RUNBOOK.md:138, 162, 192, 303: `docker compose exec db psql` only. The Azure path is the in-VNet job query (AZURE.md → Querying the database from inside the VNet).
- RUNBOOK.md:176: "(Azure: App Setting / Key Vault)" plus a `python3 -c secrets.token_urlsafe` hand-generated secret. Contradicts ADR-0008.
- RUNBOOK.md "Bad deploy / rollback": Compose-only. The Azure path is `terraform apply -var image=...sha-<good>`. If a migration changed, the downgrade runs via the migrate job with the NEW image and a `--command alembic --args downgrade <rev>` override, before rolling the app back.
- RUNBOOK.md "CI is red": "The last good image is still `:latest`, so production is unaffected". Production runs a pinned `sha-` tag (var.image), and `:latest` is rejected by validation. Reword the reason.
- RUNBOOK.md:261: "(AZURE.md → Backups on Azure)" anchor is being renamed to "Backups and restore".
- RUNBOOK.md Reference table: add Azure rows (logs, in-VNet query, current revision via migrate job `alembic current`, job execution history, Key Vault name via `terraform output`).
- INCIDENT-RESPONSE.md:70-73 Stabilize table: "Database down → Restart it" needs an Azure form (`az postgres flexible-server start/restart`, which the admin runs; verify with --help). "Leaked JWT_SECRET → Rotate it" needs a link to the RUNBOOK Azure rotation.
- INCIDENT-RESPONSE.md:77-86 Preserve evidence: `./scripts/backup.sh` is Compose-only. Azure evidence = PITR restore to a new server at the incident timestamp (non-destructive) and `az containerapp logs show` / Log Analytics export (30-day retention; export before it ages out).
- INCIDENT-RESPONSE.md:123-125 Contain: "rotate the Postgres password, update DATABASE_URL, restart the api". In Azure that is ONE `terraform apply -replace=random_password.postgres_admin` (it updates the server password and the `database-url` secret together), then `az containerapp revision restart`. Nobody hand-edits DATABASE_URL.
- INCIDENT-RESPONSE.md:235 Practice table: rotation drill is Compose-only. Keep it and label it dev; do not add an Azure drill (it would log out the live demo and cost; note it as optional with owner approval).
- BACKUP-RESTORE.md:184, 205-206: accurate (dumps are portable, `--no-owner`; Azure PITR in AZURE.md). Only touch it if the AZURE.md link needs an anchor. Add one sentence that the scripts are Compose-only and the Azure path is AZURE.md → Backups and restore.
</audit_findings>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ops-docs-match-deployment/01-01-SUMMARY.md
@docs/AZURE.md
@docs/RUNBOOK.md
@docs/INCIDENT-RESPONSE.md
@docs/BACKUP-RESTORE.md
@infra/keyvault.tf
@infra/containerapps.tf
@docs/adr/0006-terraform-state-backend.md
@docs/adr/0007-private-postgres-in-centralus.md
@docs/adr/0008-secrets-generated-into-key-vault.md
</context>

<cost_rule>
Same as 01-01: no apply/destroy, no job starts, no restarts, no rotations, no restores, no curl to the live API. Only `--help`, read-only show/list, and file edits.
</cost_rule>

<tasks>

<task type="auto">
  <name>Task 1: Add Azure paths to RUNBOOK.md and remove firewall/App-Setting steps</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>docs/RUNBOOK.md, docs/AZURE.md (rewritten), 01-01-SUMMARY.md</read_first>
  <action>
Keep the Compose procedures as they are (they were verified). For each item in <audit_findings> that concerns RUNBOOK.md, add the Azure form next to the Compose form. Use an "**Azure:**" sub-block or an extra table row, matching the existing style. Link to AZURE.md sections rather than duplicating long procedures. Specifically:
- First five minutes: add an Azure block with `curl <api_url>/health` and `/ready` (api_url from `terraform output -raw api_url`; note that the first request cold-starts), `az containerapp logs show ... --tail 50`, and `az containerapp job execution list -n careroute-migrate -o table`.
- Database unreachable table: replace the firewall row with "Azure: /ready 503 → check server state (`az postgres flexible-server show ... --query state`), the private DNS link and VNet integration; there is no firewall (ADR-0007)". Keep the sslmode row.
- API won't start / JWT_SECRET row: in Azure the secret comes from Key Vault via managed identity. Check the `jwt-secret` KV reference resolves (revision/system logs) and the identity has Secrets User. Link AZURE.md → Secrets and rotation.
- 401-for-one-user and lock-out-a-user: add the Azure in-VNet query form (AZURE.md → Querying the database from inside the VNet). The lock-out UPDATE is a write, so label it clearly.
- Invalidate everyone's tokens: Azure = `terraform apply -replace=random_password.jwt_secret` then `az containerapp revision restart`. Remove "App Setting" and the hand-generated secret for Azure (keep the Compose one for dev).
- See what an account did: Azure in-VNet query form.
- Bad deploy / rollback: Azure 2a = `terraform apply -var image=ghcr.io/kyleh777/careroute:sha-<good>`. Azure 2b = downgrade via the migrate job with `--image ...sha-<bad>` and a `--command alembic --args downgrade <rev>` override FIRST, then the Terraform image rollback. Take a PITR-able timestamp note first (PITR replaces backup.sh as the Azure safety net).
- Data loss: point to AZURE.md → Backups and restore (fix the anchor).
- CI is red: fix the `:latest` reasoning (prod runs a pinned `sha-` tag).
- Reference table: add Azure rows.
- Top-of-file "verified" paragraph: state precisely that the Azure procedures were syntax-checked against `az --help` and infra/*.tf on 2026-09-25 but not executed, to avoid cost and disruption to the live demo. Say that rehearsing them is open work.
  </action>
  <verify>
grep -nE "firewall|App Setting|Network access|JWT signing secret|Backups on Azure|still .:latest." docs/RUNBOOK.md   # expect no output
grep -nE "careroute-migrate|revision restart|random_password.jwt_secret|Querying the database from inside the VNet|terraform apply -var image" docs/RUNBOOK.md   # each present
for a in "Logs" "Secrets and rotation" "Querying the database from inside the VNet" "Backups and restore" "Deploying a new image"; do grep -q "^## $a" docs/AZURE.md || echo "missing AZURE anchor: $a"; done
  </verify>
  <acceptance_criteria>
    - No firewall-rule instruction, "App Setting", or dead AZURE.md anchor remains in RUNBOOK.md
    - Each of health/readiness, logs, migrations, secret rotation and restore has an Azure form
    - Compose procedures are unchanged (git diff shows additions around them, not rewrites)
  </acceptance_criteria>
  <done>DOCS-02: RUNBOOK's Azure procedures match the deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Route INCIDENT-RESPONSE.md containment and rotation through Terraform/Key Vault</name>
  <files>docs/INCIDENT-RESPONSE.md, docs/BACKUP-RESTORE.md</files>
  <read_first>docs/INCIDENT-RESPONSE.md, docs/RUNBOOK.md (as updated), docs/AZURE.md → Secrets and rotation</read_first>
  <action>
- Stabilize table (lines ~66-73): add Azure forms or links for Bad deploy, Database down, Leaked JWT_SECRET and Bad data change (PITR to a new server).
- Preserve evidence: add the Azure form (note the incident timestamp for PITR; export logs from Log Analytics before the 30-day retention expires). Keep backup.sh for Compose.
- Contain: rewrite the two credential bullets. Token/signing key: `terraform apply -replace=random_password.jwt_secret`, then `az containerapp revision restart`. Expect every session to 401. DB credentials: `terraform apply -replace=random_password.postgres_admin` (updates the server admin password and the `database-url` KV secret in one apply), then restart the api revision. The migrate/seed jobs read the new value on their next run, and /ready is 503 between apply and restart. Add: if the Terraform STATE or a `*.tfplan` file may have leaked, treat every secret as exposed and rotate all four (ADR-0006/0008), and review who holds Storage Blob Data Reader on the state account.
- Practice table: label the rotation drill "(dev stack)". Add a note that an Azure rotation drill logs out the live demo, costs a revision restart, and needs the owner's go-ahead.
- BACKUP-RESTORE.md: add one sentence near line 205 that the scripts target the Compose stack and the Azure path is AZURE.md → Backups and restore. No other edits.
  </action>
  <verify>
grep -nE "random_password.jwt_secret|random_password.postgres_admin|revision restart" docs/INCIDENT-RESPONSE.md   # present
grep -nE "update .DATABASE_URL." docs/INCIDENT-RESPONSE.md   # expect no output (no hand-edit of DATABASE_URL)
  </verify>
  <acceptance_criteria>
    - INCIDENT-RESPONSE.md names the Terraform `random_password` resource and the Key Vault / revision-restart step for both JWT_SECRET and the DB password
    - No step tells a human to type, generate or paste a production secret
  </acceptance_criteria>
  <done>DOCS-03: rotation steps point at the Key Vault / Terraform path.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-doc audit against ADR-0006/0007/0008 (ROADMAP criterion 4)</name>
  <files>(read-only audit; fix any hit in the file where it occurs)</files>
  <read_first>docs/adr/0006, 0007, 0008</read_first>
  <action>
Grep all three phase docs plus README.md for contradiction patterns and fix any hit:
`firewall`, `public access`, `0.0.0.0`, `eastus` (allowed only in the tfstate context), `App Setting`, `token_urlsafe` (allowed only in Compose/dev context), `--admin-password`, `az keyvault secret set` (hand-set secret), `psql .*azure`, `sslmode=disable`, `Nothing (here|there) has been provisioned`, `no infrastructure`, `AKS|Kubernetes|kubectl|helm`, `:latest` used as a deploy target for Azure. Record the final grep output (should be empty or only allowed contexts) in the SUMMARY, with one line per allowed hit explaining why it is allowed.
  </action>
  <verify>
grep -nEi "firewall|0\.0\.0\.0|App Setting|--admin-password|keyvault secret set|sslmode=disable|has been provisioned|no infrastructure|\bAKS\b|kubernetes|kubectl|helm" README.md docs/AZURE.md docs/RUNBOOK.md docs/INCIDENT-RESPONSE.md
grep -nE "eastus|token_urlsafe" README.md docs/AZURE.md docs/RUNBOOK.md docs/INCIDENT-RESPONSE.md   # review each hit's context
  </verify>
  <acceptance_criteria>
    - First grep returns no lines, except a sentence stating there is no firewall (allowed, explained in SUMMARY)
    - Every `eastus` hit refers to the tfstate storage; every `token_urlsafe` hit is in a Compose/dev context
  </acceptance_criteria>
  <done>A reviewer grepping the docs finds nothing that contradicts ADR-0006/7/8.</done>
</task>

</tasks>

<verification>
- [ ] All task verify commands pass
- [ ] `git diff --stat` lists only docs/RUNBOOK.md, docs/INCIDENT-RESPONSE.md, docs/BACKUP-RESTORE.md (plus 01-01's files from wave 1)
- [ ] Markdown links from RUNBOOK/INCIDENT-RESPONSE/BACKUP-RESTORE into AZURE.md resolve to existing headings
- [ ] No cost-incurring command executed
</verification>

<success_criteria>
- ROADMAP Phase 1 criteria 2, 3, 4 met (with the Azure procedures honestly marked syntax-checked rather than run live, per the user's cost instruction)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ops-docs-match-deployment/01-02-SUMMARY.md`
</output>
