---
phase: 02-least-privilege-database-roles
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - docs/adr/0010-per-workload-identities-and-db-roles.md
  - docs/adr/README.md
  - docs/adr/0007-private-postgres-in-centralus.md
  - docs/AZURE.md
  - docs/RUNBOOK.md
  - docs/INCIDENT-RESPONSE.md
  - docs/BACKUP-RESTORE.md
  - README.md
autonomous: true
requirements: [DB-01]
user_setup: []

must_haves:
  truths:
    - "The docs say which workload uses which role and identity, and how to rotate each password"
    - "No doc still says the app connects as the server admin"
    - "Every job-override example uses the right secret for its purpose (app URL for reads, migrate URL for alembic, admin only via db-bootstrap)"
  artifacts:
    - path: docs/adr/0010-per-workload-identities-and-db-roles.md
      provides: "Decision record amending ADR-0008"
---

<objective>
Bring the docs in line with the live role/identity model (criterion 5), recording the decision as ADR-0010.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-least-privilege-database-roles/02-CONTEXT.md
@.planning/phases/02-least-privilege-database-roles/02-01-SUMMARY.md
@.planning/phases/02-least-privilege-database-roles/02-02-SUMMARY.md
@docs/adr/0008-secrets-generated-into-key-vault.md
@docs/adr/0007-private-postgres-in-centralus.md
@docs/AZURE.md
@docs/RUNBOOK.md
</context>

<cost_rule>Docs only. No Azure calls.</cost_rule>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0010, ADR index, ADR-0007 consequence</name>
  <files>docs/adr/0010-per-workload-identities-and-db-roles.md, docs/adr/README.md, docs/adr/0007-private-postgres-in-centralus.md</files>
  <action>
ADR-0010 in the house format (Status/Date/Context/Decision/Consequences, short): the two roles and their privileges, the bootstrap job as the only admin user, per-workload identities, per-secret Key Vault RBAC (amends ADR-0008's single identity), and staged rollout flags. Consequences: rotation of the role passwords requires a bootstrap run; anyone who can start db-bootstrap effectively holds admin (the Phase 1 job-override finding); TRUNCATE needs the owner role; after a restore, run db-roles.
ADR-0007: replace "The app currently connects as the server admin; a dedicated least-privilege role is open work." with a pointer to ADR-0010. Add ADR-0010 to the index.
  </action>
  <verify>grep -n "connects as the server admin" docs/adr/0007*.md   # no output</verify>
  <done>The decision is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: AZURE.md, RUNBOOK, INCIDENT-RESPONSE, BACKUP-RESTORE, README</name>
  <files>docs/AZURE.md, docs/RUNBOOK.md, docs/INCIDENT-RESPONSE.md, docs/BACKUP-RESTORE.md, README.md</files>
  <action>
- AZURE.md Architecture: add the three identities, the db-bootstrap job and the roles table (role → privileges → used by → KV secret → identity).
- AZURE.md Secrets and rotation: extend the table with database-url-admin, database-url-migrate, db-migrate-password and db-app-password (read by → identity). Add rotation for `random_password.pg_app` / `pg_migrate`: apply → **immediately** run `careroute-db-bootstrap` (it syncs the DB password to Key Vault) → restart the API (for app). Explain the window: after the apply, Key Vault holds the new password but the DB has the old one until bootstrap runs, and Container Apps may auto-refresh within 30 min, so run bootstrap right away. Keep the postgres_admin rotation, noting it now only affects the bootstrap job.
- AZURE.md Querying the database: JOB_ENV and examples. Reads go through the **seed** job override (app role, `DATABASE_URL=secretref:database-url`, identity seed-id). `alembic current` and the deploy-migration override go through migrate with `secretref:database-url-migrate`. Admin-only diagnostics (pg_stat_activity across users) go through the db-bootstrap override with `secretref:database-url-admin`, marked break-glass. Update the "connects as the server admin" warning. Double-check each job's secret names match infra, since an override can only reference secrets defined on that job.
- AZURE.md Deploying a new image: the migrate override uses the migrate URL secret.
- RUNBOOK.md: lockout/audit queries run as the app role; add a "Database roles" subsection (who is who, symptoms of a missing grant: `permission denied for table X` in api logs → run db-bootstrap, which re-applies grants); local `seed --reset` now via `docker compose run --rm migrate python scripts/seed.py --reset`; the Compose ordering is db → db-roles → migrate → api (first-five-minutes: `docker compose logs db-roles`).
- INCIDENT-RESPONSE.md Contain: DB credentials exposed → which role? The app password: replace pg_app + bootstrap + restart. The admin: replace postgres_admin (affects only bootstrap). Keep "state leaked → rotate all", now six secrets.
- BACKUP-RESTORE.md: after `restore.sh`, run `docker compose run --rm db-roles` (restored objects are owned by the restoring superuser; the result is from 02-01 Task 4).
- README.md: the Compose section mentions the role split and the db-roles step; update any `exec api python scripts/seed.py --reset` usages.
  </action>
  <verify>
grep -rnE "exec api python scripts/seed.py --reset|connects as the server admin|It connects as the \*\*server admin\*\*" README.md docs/   # no output
grep -n "careroute_app\|careroute_migrate\|db-bootstrap" docs/AZURE.md docs/RUNBOOK.md | head
  </verify>
  <done>Criterion 5: the docs describe the roles, who uses which, and rotation for each.</done>
</task>

</tasks>

<verification>
- [ ] All grep checks pass
- [ ] Every secret name in the docs exists in infra/keyvault.tf
- [ ] Every job-override example references only secrets defined on that job in infra/containerapps.tf
</verification>

<success_criteria>ROADMAP Phase 2 criterion 5.</success_criteria>

<output>Create `.planning/phases/02-least-privilege-database-roles/02-03-SUMMARY.md`</output>
