---
phase: 03-automated-ordered-deploys
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified: [docs/adr/0011-ci-deploys-via-terraform.md, docs/adr/README.md, docs/AZURE.md, docs/RUNBOOK.md, docs/INCIDENT-RESPONSE.md, README.md]
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]
must_haves:
  truths:
    - "The docs say deploys are automatic and describe the ordered stages, the drill and rollback"
    - "The accepted risk (CI can read every secret; owner-equivalent within careroute-rg, bounded by the RBAC condition) is recorded in an ADR and in the runbook's who-can-read-secrets list"
    - "Manual applies document the required image vars and TF_VAR_operator_object_id"
---
<objective>Document the automated deploy and its accepted risk (criterion 5).</objective>
<tasks>
<task type="auto">
  <name>Task 1: ADR-0011 + docs</name>
  <action>
- ADR-0011 "CI deploys by running Terraform, in two stages": context (DEPLOY-01, the ordering hazard), decision (stages, required image vars, OIDC environment subject, separate CI RG/root, constrained RBAC Admin, container-scoped state access), consequences (**accepted**: CI reads every secret via state + KV Secrets Officer; the alternative image-only model was considered and declined 2026-09-26; mitigations; a compromised `main` is a compromised deployment). Add it to the ADR index.
- AZURE.md "Deploying a new image": CI does it on every push to main (describe the stages); the manual fallback = the same two applies with the required vars; humans applying infra changes must pass `-var app_image=$(terraform output -raw app_image) -var migrate_image=$(terraform output -raw migrate_image)` and set TF_VAR_operator_object_id. Architecture: add careroute-ci-rg / careroute-ci-id. Update First-time deployment (both roots; image vars).
- RUNBOOK: "Bad deploy / rollback" → `gh workflow run ci.yml --ref main -f image_tag=sha-<good>` when no schema change (a migrate with an older image fails "Can't locate revision" by design, so schema rollbacks stay manual, per 2b); "CI is red" rows for the deploy job (OIDC login failure, state lock, migrate failed, smoke failed); a new "Who can read production secrets" list: Key Vault Secrets Officers (operator, careroute-ci-id), anyone with Storage Blob Data Reader/Contributor on the state container (operator, careroute-ci-id), each workload identity for its own secrets, and anyone who can push to main or edit ci.yml on main (via CI).
- INCIDENT-RESPONSE: the secrets-exposure path includes "compromised main branch/CI"; containment = disable the `production` environment or delete the federated credential (`az identity federated-credential delete`) and rotate all secrets.
- README: the CI section mentions the deploy job.
  </action>
  <verify>grep -rn "variables.tf.*image\|Set \`default\` for \`image\`" docs/ README.md   # no stale manual-bump instructions</verify>
  <done>Criterion 5 is documented.</done>
</task>
</tasks>
<output>03-03-SUMMARY.md</output>
