---
phase: 03-automated-ordered-deploys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [infra/variables.tf, infra/containerapps.tf, infra/keyvault.tf, infra/outputs.tf, infra/ci/main.tf, infra/ci/variables.tf, infra/ci/outputs.tf, infra/ci/backend.hcl, infra/ci/.terraform.lock.hcl]
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "The app image and the migrate image are separate required variables; an apply without them fails instead of rolling back"
    - "Key Vault Secrets Officer is held by the human operator AND the CI identity, independent of who runs Terraform"
    - "careroute-ci-id exists in careroute-ci-rg with exactly: Contributor + constrained RBAC Admin on careroute-rg, and Storage Blob Data Contributor on the tfstate container"
    - "Its only federated credential subject is repo:KyleH777/careroute:environment:production"
---

<objective>
Make the Terraform safe to run from CI (the ordering split, a stable secrets-officer set), and create the CI identity in its own human-applied root.
</objective>

<context>
@.planning/phases/03-automated-ordered-deploys/03-CONTEXT.md
@.planning/phases/03-automated-ordered-deploys/03-RESEARCH.md
@infra/main.tf @infra/variables.tf @infra/keyvault.tf @infra/containerapps.tf @infra/outputs.tf
</context>

<cost_rule>Local validate/plan are free. Applies happen only at the checkpoint. The new resources (RG, identity, federated credential, role assignments) cost nothing.</cost_rule>

<tasks>
<task type="auto">
  <name>Task 1: Main config refactor</name>
  <action>
- variables.tf: replace `image` with `app_image` and `migrate_image` (type string, **no default**, the same `:latest` validation on both). Descriptions state that deploys set migrate first (ADR-0001) and that humans pass the live values from `terraform output`. Add `operator_object_id` (string, no default, validated as a GUID): the human operator's Entra object ID, supplied via `TF_VAR_operator_object_id`. Add `ci_identity_name` (default `careroute-ci-id`) and `ci_resource_group` (default `careroute-ci-rg`).
- containerapps.tf: API, seed and db_bootstrap use `var.app_image`; migrate uses `var.migrate_image`.
- keyvault.tf: `data "azurerm_user_assigned_identity" "ci"` (by name/RG). `deployer_secrets_officer` becomes `for_each = { operator = var.operator_object_id, ci = data.azurerm_user_assigned_identity.ci.principal_id }`, with `moved { from = azurerm_role_assignment.deployer_secrets_officer to = azurerm_role_assignment.deployer_secrets_officer["operator"] }`. `time_sleep.rbac_propagation` depends on the map.
- outputs.tf: `app_image`, `migrate_image` (live values, read by CI stage 1 and by humans).
- main.tf header: update the first-time/apply instructions (required vars, TF_VAR_operator_object_id).
  </action>
  <verify>cd infra && terraform fmt -check && terraform validate</verify>
  <done>The config validates; the ordering hazard is structurally closed.</done>
</task>

<task type="auto">
  <name>Task 2: infra/ci root (human-applied only)</name>
  <action>
New root `infra/ci/` with the same azurerm backend (RG careroute-tfstate-rg, container tfstate, key `careroute-ci.tfstate`, use_azuread_auth) and its own lock file:
- `azurerm_resource_group.ci` "careroute-ci-rg" (centralus; tags managed-by=terraform, purpose=ci-identity).
- `azurerm_user_assigned_identity.ci` "careroute-ci-id".
- `azurerm_federated_identity_credential.github_production`: issuer `https://token.actions.githubusercontent.com`, audience `["api://AzureADTokenExchange"]`, subject `repo:${var.github_repo}:environment:production` (var default `KyleH777/careroute`), `user_assigned_identity_id` (not the deprecated parent_id).
- Role assignments for its principal: Contributor on the careroute-rg ID (data source); Role Based Access Control Administrator on careroute-rg with `condition_version = "2.0"` and the RESEARCH "Constrain roles" condition, filled in with the Key Vault Secrets User + Officer GUIDs (looked up with `az role definition list`, hardcoded with a comment); Storage Blob Data Contributor on the `tfstate` container ID (`<storage account id>/blobServices/default/containers/tfstate`, storage account via a data source using the name from ../backend.hcl passed as a variable).
- Outputs: `client_id`, `tenant_id`, `principal_id`. No secrets exist here.
- A header comment: why a separate RG and root (CI must not be able to edit its own identity/credentials), and that CI never applies this root.
  </action>
  <verify>cd infra/ci && terraform init -backend-config=../backend.hcl && terraform fmt -check && terraform validate && terraform plan   # expect +6, 0 destroy</verify>
  <done>The CI identity root validates and plans cleanly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Both configs plan cleanly.</what-built>
  <how-to-verify>Approve: (1) `terraform apply` in infra/ci (creates the RG, identity, federated credential and 3 role assignments); (2) `terraform apply` in infra with `-var app_image=$(terraform output -raw ...)`-equivalents (current live `sha-da96d74`) and `TF_VAR_operator_object_id`. Expected main plan: 1 add (Secrets Officer for CI), the moved operator assignment, no image or other changes.</how-to-verify>
  <resume-signal>"approved"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Apply both and verify</name>
  <action>Apply infra/ci, then the main config (review the plan first: it must show only the CI Secrets Officer add and the move). Then: `az role assignment list --assignee <ci principal> --all -o table` shows exactly the 4 expected assignments (the 3 from infra/ci + KV Secrets Officer), the RBAC Admin one with its condition; `az identity federated-credential list` shows the single production subject. Commit both roots.</action>
  <done>The CI identity exists with the planned, bounded permissions.</done>
</task>
</tasks>

<output>03-01-SUMMARY.md</output>
